{
  "name": "intersection-observer",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/uepon24/intersection-observer-test.git",
  "license": "MIT",
  "scripts": {
    "start": "yarn run copy && yarn run server",
    "build": "yarn run copy",
    "copy": "cp index.html intersection-observer.js script.js style.css dist && cp -r img dist",
    "server": "browser-sync start --server --files='dist/index.html'"
  },
  "devDependencies": {
    "browser-sync": "^2.24.7"
  }
}
